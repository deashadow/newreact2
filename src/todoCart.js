/*import {useSelector, useDispatch} from 'react-redux';
import {incr, decr, signIn} from './actions';
import {Provider, connect} from 'react-redux';

function todoCart(props) {
  const counter = useSelector( state => state.counter)
  const islogged = useSelector( state => state.islogged)
  const dispatch = useDispatch();
  return (
  <div className="App">
  <div><button onClick={()=>dispatch(incr())} >Add</button></div>
  <div><button onClick={()=>dispatch(decr())} >Sub</button></div>
  <div><button onClick={()=>dispatch(signIn())} >Login</button></div>
  <h3>counter is {counter}</h3>
  { islogged ? <h3>Logged In</h3> : <h3>Logged Out</h3>}
  </div>
  );
 } 

 export default todoCart;*/
